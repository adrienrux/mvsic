.schedule-view style="background-image: url({{ schedule.festival.background_image }});"
  .festival-wrapper
    .festival-pane.animated.fadeInUp
      a.festival-name ng-href="/festivals/{{schedule.festival.slug}}"
        | {{ schedule.festival.name }}
      .festival-links
        a ng-href="{{ tweetHref }}" target="_blank"
          i.icon.ion-social-twitter
        a ng-href="{{ schedule.festival.website }}" target="_blank"
          i.icon.ion-earth
      .festival-info
        .festival-location
          | {{ schedule.festival.location }}
        .festival-date
          | {{ schedule.festival.start_date }} &mdash; {{ schedule.festival.end_date }}
      .festival-description
        | After the initial scare of Ezoo not procuring a permit due to drug related incidents last year. Made Events have come through and have announced a top-notch line up and have even prepared an anti-drug video for this year’s festivity at Randall’s Island. The line-up features house and trap heavy weights and there is even a stage dedicated to Vinyl only!

  .events-wrapper.an
    .events-pane.animated.fadeInUp
      .artist-list
        .day ng-repeat="day in eventsByDay"
          .label
            | {{day.date}}
          .event ng-repeat="event in day.events"
            .artist
              .button.play ng-click="$emit('selectArtist', event.artist)" play
              .name
                | {{ event.artist.name }}
            .info
              .location
                | {{ event.venue.name }}
              .settime ng-show="event.start_time"
                | {{ timeDisplay(event.start_time) }} - {{ timeDisplay(event.end_time) }}
